streamlit>=1.37      # 1.37+ for st.fragment (scoped reruns)
altair>=5.0          # interactive Vega-Lite charts (ships with streamlit)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
openpyxl>=3.1
xlrd>=2.0

# Optional — only needed to import PDF account statements
# pdfplumber>=0.10
